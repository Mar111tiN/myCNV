{
  "__default__": {
    "t": "00:45",
    "mem": "1000",
    "nodes": 1,
    "partition": "debug",
    "threads": "{threads}",
  },
  "get_coverage": {
    "t": "00:35",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}.{wildcards.chrom}"
  },
  "chrom_coverage": {
    "t": "00:15",
    "mem": 200,
    "name": "{rule}:{wildcards.sample}.{wildcards.chrom}"
  },
  "combine_PON_coverage": {
    "t": "00:10",
    "mem": 2000,
    "name": "{rule}"
  },
  "hetero_SNP": {
    "t": "01:30",
    "mem": 200,
    "partition": "short",
    "name": "{rule}:{wildcards.sample}.{wildcards.chrom}"
  },
  "snp2EB": {
    "t": "02:30",
    "mem": 500,
    "partition": "short",
    "name": "{rule}:{wildcards.sample}.{wildcards.chrom}"
  }
}