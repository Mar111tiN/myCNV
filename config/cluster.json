{
  "__default__": {
    "t": "00:45",
    "mem": "1000",
    "nodes": 1,
    "partition": "debug",
    "threads": "{threads}",
  },
  "get_coverage": {
    "t": "00:35",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}.{wildcards.chrom}"
  },
  "chrom_coverage": {
    "t": "00:15",
    "mem": 500,
    "name": "{rule}:{wildcards.sample}.{wildcards.chrom}"
  },
  "combine_PON_coverage": {
    "t": "00:10",
    "mem": 2000,
    "name": "{rule}"
  },
  "hetero_SNP": {
    "t": "02:45",
    "mem": 500,
    "partition": "short",
    "name": "{rule}:{wildcards.sample}.{wildcards.chrom}"
  },
  "SNP2EB": {
    "t": "02:30",
    "mem": 500,
    "partition": "short",
    "name": "{rule}:{wildcards.sample}.{wildcards.chrom}"
  },
  "combine_CNV": {
    "t": "00:05",
    "mem": 2500,
    "partition": "debug",
    "name": "{rule}:{wildcards.sample}"
  },
  "rolling_cov": {
    "t": "00:10",
    "mem": 2500,
    "partition": "debug",
    "name": "{rule}:{wildcards.sample}"
  }
}
